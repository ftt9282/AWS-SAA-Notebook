Allows you to attach the same EBS Volume to multiple [[EC2]] instances within an AZ. This is useful to maintain higher availability in a clustered Linux environment, or if the application must maintain concurrent write operations

Can only maintain **up to 16 EC2 instances at a time**.