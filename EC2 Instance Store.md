Kind of like an [[EBS Volume]], but has a higher performance. An EC2 Instance store is a hardware disk that is attached to your [[EC2]] instance.

Even though an EC2 instance is a virtual machine, it is still attached to a real hardware server. Some of these servers do actually have disk space that is attached to the server.

EC2 Instance Store has an ephemeral database, so it loses its storage if it's stopped